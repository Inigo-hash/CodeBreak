pygame>=2.6.1
# Used by src/ui/particles.py to lift the painted motes out of the main menu
# background plate. The menu degrades gracefully without it (static plate,
# procedurally scattered particles), but it is wanted for the intended look.
numpy>=2.0
pytmx>=3.32
